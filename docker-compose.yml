version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=development

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: ecms
      POSTGRES_USER: ecms
      POSTGRES_PASSWORD: ecms
    ports:
      - "5432:5432"
